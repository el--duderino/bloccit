<%= form_for [topic, post, comment], remote: true do |f| %>
  <%= f.text_area :body, rows: 8, columns: 10, class: 'form-control', placeholder: "Enter comment" %>
  <%= f.submit "Create comment", class: 'btn btn-success' %>
<% end %>